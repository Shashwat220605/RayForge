# Build notes

Keep build instructions aligned with the current C++ toolchain and project layout. When dependencies or compiler requirements change, update the README and validate a clean build from a fresh checkout.